#include <stdio.h>
#include <stdlib.h>
#i

int main()
{
    int largo,c;
    char pal [30];
    scanf("%s",pal);
    largo=strlen(pal);
    for(c=0,c=largo,c++){
        if (pal[c]>=97&&pal[c]<=122){
            pal[c]=pal[c]-32;
        }
        if(pal[c]=´ñ´);
            pal[c]=´Ñ´;
        }
      printf("%s",pal);
    }

    printf("\n\n");
    system("pause");
    return 0;
}
