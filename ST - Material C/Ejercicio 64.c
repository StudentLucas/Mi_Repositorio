#include <stdio.h>
#include <stdlib.h>

int main()
{
    char n1[6]="Carla", n2[20]="Carla";
    int l1,l2;
    l1=strlen(n1);
    l2=strlen(n2);
    printf("%i\n%i\n",l1,l2);

    return 0;
}
