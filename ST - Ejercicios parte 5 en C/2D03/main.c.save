#include <stdio.h>
#include <stdlib.h>
#define pru 3

int main()
{
    int c, entre=0, tot=0, ed, menor=0, toted=0, cua=0;
    float prom, porc, sue ;
    for (c=1;c<=pru;c++){
            printf("\nIngrese sueldo ");
            scanf("%f",&sue);
            printf("Ingrese edad ");
            scanf("%i",&ed);
            if (sue>600){
                if (sue<900){
                    entre=entre+1;
                }
            }
            if (sue<500){
                menor=menor+1;
            }
            if (sue>1000){
                cua=cua+1;
            }else{
                if (ed>50){
                    cua=cua+1;
                }
            }
            tot=tot+sue;
            toted=toted+ed;
    }
    porc=menor*100/pru;
    prom=toted/pru;

    printf("\n%i Ganan entre $600 y $900",entre);


    printf("\n");
    return 0;
}
